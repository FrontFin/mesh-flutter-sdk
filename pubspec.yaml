name: mesh_sdk
description: "Flutter library for integrating with Mesh Connect."
version: 0.0.1
homepage: https://meshconnect.com

environment:
  sdk: ^3.9.2

dependencies:
  collection: ^1.19.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  logging: ^1.3.0
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.2
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  generate: true
